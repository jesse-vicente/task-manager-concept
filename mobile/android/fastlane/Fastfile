default_platform(:android)

platform :android do
  lane :test do
    increment_version_code

    increment_version_name

    commit_android_version_bump(
      force: true
    )

    push_to_git_remote
  end
end